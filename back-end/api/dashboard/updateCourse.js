import updateCourseController from "../../controllers/systemControllers/updateCourseController.js";

const updateCourse = (req, res) => {
  updateCourseController(req, res);
};

export default updateCourse;
