import getAnalysticController from "../../controllers/systemControllers/getAnalysticController.js";

const getAnalystic = (req, res) => {
  getAnalysticController(req, res);
};

export default getAnalystic;
